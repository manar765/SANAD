# دليل وحالات الاختبار اليدوي الشامل لمنصة سَنَد (SANAD Manual Testing Guide)

> **دليل عملي تفصيلي لاختبار كافة مسارات وصفحات ومعاملات النظام خطوة بخطوة.**  
> تم إعداد هذا الدليل لتمكين مسؤولي الجودة والمطورين من فحص المنصة يدوياً وتجربة كل الصلاحيات والعمليات الذرية والميزات الذكية.

---

## 📋 فهرس مسارات الاختبار (Test Suites Index)

1. [إعداد بيئة الاختبار والحسابات التجريبية](#1-إعداد-بيئة-الاختبار-والحسابات-التجريبية)
2. [المجموعة الأولى: المصادقة، الأمان، وإدارة الحسابات (Auth & Security)](#المجموعة-الأولى-المصادقة-الأمان-وإدارة-الحسابات)
3. [المجموعة الثانية: الواجهة العامة والتصميم المتجاوب (Public & Navigation)](#المجموعة-الثانية-الواجهة-العامة-والتصميم-المتجاوب)
4. [المجموعة الثالثة: دورة التبرعات العينية (In-Kind Donations Lifecycle)](#المجموعة-الثالثة-دورة-التبرعات-العينية)
5. [المجموعة الرابعة: إدارة المستودع والمخزون (Warehouse & Inventory)](#المجموعة-الرابعة-إدارة-المستودع-والمخزون)
6. [المجموعة الخامسة: سجل المستفيدين والذكاء الاصطناعي (Beneficiaries & AI)](#المجموعة-الخامسة-سجل-المستفيدين-والذكاء-الاصطناعي)
7. [المجموعة السادسة: محرك القواعد ومكتب التحقق (Rules Engine & Verification)](#المجموعة-السادسة-محرك-القواعد-ومكتب-التحقق)
8. [المجموعة السابعة: عمليات التوزيع الميداني والخصم الذري (Field Distributions)](#المجموعة-السابعة-عمليات-التوزيع-الميداني-والخصم-الذري)
9. [المجموعة الثامنة: طلبات المساعدة الذاتية للمستفيدين (Assistance Requests)](#المجموعة-الثامنة-طلبات-المساعدة-الذاتية-للمستفيدين)
10. [المجموعة التاسعة: إدارة المستخدمين والصلاحيات (Users & Roles RBAC)](#المجموعة-التاسعة-إدارة-المستخدمين-والصلاحيات)
11. [المجموعة العاشرة: لوحة المؤشرات، التقارير وسجل الرقابة (Dashboard & Audit)](#المجموعة-العاشرة-لوحة-المؤشرات-التقارير-وسجل-الرقابة)

---

## 1. إعداد بيئة الاختبار والحسابات التجريبية

### تشغيل الخادم محلياً
افتح الطرفية (Terminal) في مجلد المشروع ونفذ:
```bash
npm run dev
# أو
npm start
```
* الرابط المحلي: **`http://localhost:3000`**

### بيانات الحسابات المعتمدة للاختبار:
| الدور | البريد الإلكتروني | كلمة المرور | الاستخدام |
| :--- | :--- | :--- | :--- |
| **المشرف العام (Admin)** | `admin@sanad.com` | `Sanad@2026` | كامل صلاحيات المنصة والمستودع والموافقة والرقابة |
| **متبرع فرد (Donor Individual)** | يتم إنشاؤه عبر `/signup` | مثل: `P@ssword123` | تسجيل تبرعات عينية ومتابعة حالتها |
| **متبرع مؤسسة (Donor Org)** | يتم إنشاؤه عبر `/signup` | مثل: `P@ssword123` | تسجيل تبرعات كبرى باسم شركة/جمعية |
| **مستفيد (Beneficiary)** | يتم إنشاؤه عبر `/signup` | مثل: `P@ssword123` | تقديم طلبات مساعدة ذاتية ومتابعتها |

---

## المجموعة الأولى: المصادقة، الأمان، وإدارة الحسابات

### `TC-AUTH-01`: تسجيل حساب متبرع فردي جديد
* **الصفحة:** `/signup`
* **الدور:** زائر (Guest)
* **الخطوات:**
  1. ادخل إلى صفحة التسجيل عبر زر «تسجيل جديد» في شريط التنقل.
  2. أدخل البيانات التالية:
     * الاسم الأول: `طارق` | الاسم الأخير: `منصور`
     * البريد: `tarek.donor@test.com`
     * الهاتف: `01011223344`
     * نوع الحساب: اختر **«متبرع»**
     * نوع المتبرع: اختر **«فرد»**
     * كلمة المرور: `Sanad@Pass2026`
  3. اضغط زر «إنشاء الحساب».
* **النتيجة المتوقعة:**
  - ظهور رسالة نجاح خضراء تفيد بإنشاء الحساب.
  - تسجيل الدخول تلقائياً (أو طلب التحقق حسب إعدادات البيئة).
  - توجيه المستخدم لصفحة التبرعات `/donations` وظهور اسم `طارق منصور` في شريط الرأس.

---

### `TC-AUTH-02`: تسجيل حساب متبرع باسم منظمة/مؤسسة
* **الصفحة:** `/signup`
* **الدور:** زائر (Guest)
* **الخطوات:**
  1. في صفحة `/signup`، اختر الدور **«متبرع»**.
  2. اختر نوع المتبرع **«مؤسسة أو شركة»**.
  3. تأكد من ظهور حقل جديد إلزامي باسم **«اسم المؤسسة / المنظمة»**.
  4. املأ الحقول:
     * الاسم: `مروان` `خالد`
     * البريد: `marwan@charity-org.eg`
     * الهاتف: `01233445566`
     * اسم المؤسسة: `مؤسسة الأمل الخيرية`
     * كلمة المرور: `Sanad@Pass2026`
  5. اضغط زر «إنشاء الحساب».
* **النتيجة المتوقعة:**
  - حفظ بيانات المتبرع وربطه بملف `donor_profiles` كنوع `organization`.
  - تحويله لصفحة `/donations` مع تفعيل صلاحيات المتبرع.

---

### `TC-AUTH-03`: تسجيل حساب مستفيد (Beneficiary)
* **الصفحة:** `/signup`
* **الدور:** زائر (Guest)
* **الخطوات:**
  1. في صفحة `/signup`، اختر الدور **«مستفيد»**.
  2. لاحظ اختفاء حقول المؤسسات وبقاء الحقول الأساسية.
  3. أدخل البيانات:
     * الاسم: `صابر` `عبد الله`
     * البريد: `saber.beneficiary@test.com`
     * الهاتف: `01122334455`
     * كلمة المرور: `Sanad@Pass2026`
  4. اضغط زر «إنشاء الحساب».
* **النتيجة المتوقعة:**
  - يتم إنشاء المستخدم بدور `beneficiary` وإنشاء سجل مرتبط في `beneficiary_profiles`.
  - التحويل التلقائي لصفحة طلبات المساعدة `/request-assistance`.

---

### `TC-AUTH-04`: فحص حماية تكرار البريد والتحقق من صحة المدخلات
* **الصفحة:** `/signup`
* **الخطوات:**
  1. أعد محاولة التسجيل بنفس البريد `tarek.donor@test.com`.
  2. جرب إدخال رقم هاتف غير صحيح (أقل من 8 أرقام مثل `123`).
  3. جرب إدخال كلمة مرور قصيرة (أقل من 8 أحرف مثل `12345`).
* **النتيجة المتوقعة:**
  - رفض الطلب محلياً أو من الخادم برسائل خطأ عربية واضحة («هذا البريد مسجل بالفعل»، «يرجى إدخال رقم هاتف صحيح»، «كلمة المرور يجب ألا تقل عن 8 أحرف»).

---

### `TC-AUTH-05`: تسجيل الدخول القياسي مع تفعيل خيار «تذكرني»
* **الصفحة:** `/login`
* **الخطوات:**
  1. ادخل بالبريد: `admin@sanad.com` وكلمة المرور: `Sanad@2026`.
  2. حدد علامة الصح على خيار **«تذكرني»**.
  3. اضغط «تسجيل الدخول».
* **النتيجة المتوقعة:**
  - الدخول بنجاح والتوجيه إلى لوحة المؤشرات المركزية `/dashboard`.
  - الجلسة تُحفظ في الكوكيز الآمنة `sanad_session` بصلاحية ممتدة (30 يوماً).
  - تسجيل حركة `login_success` في سجل الرقابة `audit_logs`.

---

### `TC-AUTH-06`: اختبار الحماية ضد التخمين (Rate Limiting)
* **الصفحة:** `/login`
* **الخطوات:**
  1. اكتب أي بريد مثل `wrong@sanad.com` وكلمة مرور خاطئة.
  2. اضغط «تسجيل الدخول» 5 مرات متتالية بسرعة.
* **النتيجة المتوقعة:**
  - في المحاولة السادسة، يرفض النظام الطلب بكود `429 Too Many Requests`.
  - ظهور رسالة تنبيه: «لقد تجاوزت الحد الأقصى لمحاولات تسجيل الدخول. يرجى المحاولة بعد 15 دقيقة».

---

### `TC-AUTH-07`: إعداد وتفعيل التحقق الثنائي (MFA / 2FA TOTP)
* **الصفحة:** `/profile`
* **الدور:** مستخدم مسجل الدخول
* **الخطوات:**
  1. اذهب لصفحة الملف الشخصي `/profile`.
  2. انزل إلى قسم **«الأمان والمصادقة الثنائية (MFA)»**.
  3. اضغط زر «إعداد المصادقة الثنائية».
  4. تظهر نافذة منبثقة تحتوي على:
     - رمز المفتاح السري (Secret Key).
     - رابط / باركود تهيئة تطبيقات المصادقة (مثل Google Authenticator أو Authy).
     - قائمة برموز الاسترداد الاحتياطية (Recovery Codes).
  5. انسخ المفتاح السري وأضفه لتطبيق Google Authenticator في هاتفك (أو استخدم موقع خفيف مثل totp.danhersam.com للتجربة السريعة).
  6. أدخل الرمز المكون من 6 أرقام المولّد واضغط «تأكيد وتفعيل».
* **النتيجة المتوقعة:**
  - تفعيل التحقق الثنائي، وتحول حالة الـ MFA إلى «مفعل».
  - حفظ المفتاح مشفراً بـ AES-256 وتخزين تجزئات رموز الاسترداد.

---

### `TC-AUTH-08`: تسجيل الدخول مع تحدي المصادقة الثنائية (MFA Challenge)
* **الصفحة:** `/login`
* **الخطوات:**
  1. سجل الخروج من الحساب المفعل عليه الـ MFA.
  2. اذهب إلى `/login` واكتب البريد وكلمة المرور واضغط دخول.
  3. لاحظ أن النظام لا يدخلك مباشرة بل ينقلك إلى واجهة تحدي المصادقة الثنائية:
     - حقل إدخال رمز التحقق (6 أرقام).
     - خيار «استخدام رمز الاسترداد الاحتياطي».
  4. أدخل الرمز الخاطئ `000000` واضغط تأكيد -> ظهور خطأ «رمز التحقق غير صحيح».
  5. أدخل الرمز الصحيح من التطبيق -> نجاح الدخول والتوجيه للوحة المناسبة.

---

### `TC-AUTH-09`: استعادة كلمة المرور عبر الرابط الآمن (Forgot Password)
* **الصفحة:** `/forgot-password`
* **الخطوات:**
  1. افتح `/forgot-password` واكتب البريد الإلكتروني للمستخدم.
  2. اضغط «إرسال رابط الاستعادة».
  3. في بيئة التطوير، يقوم الخادم بطباعة رابط إعادة التعيين في طرفية الـ Terminal ويرجعه في الاستجابة للاختبار السريع:
     `http://localhost:3000/reset-password?token=xxxx...`
  4. انسخ الرابط وافتحه في المتصفح.
  5. أدخل كلمة مرور جديدة (مثال: `NewSanad@2026`) وأكدها.
  6. اضغط «تعيين كلمة المرور الجديدة».
* **النتيجة المتوقعة:**
  - نجاح تغيير كلمة المرور وتشفيرها عبر خوارزمية Scrypt.
  - إبطال رمز الاستعادة فوراً لمنع إعادة استخدامه.
  - إنهاء أي جلسات نشطة سابقة للمستخدم لضمان الأمان.
  - التمكن من تسجيل الدخول بكلمة المرور الجديدة بنجاح.

---

### `TC-AUTH-10`: إدارة الجلسات النشطة وتسجيل الخروج من كافة الأجهزة
* **الصفحة:** `/profile`
* **الخطوات:**
  1. افتح متصفحاً ثانياً (أو وضع التصفح المتخفي Incognito) وسجل الدخول بنفس الحساب.
  2. عد للمتصفح الأول وافتح صفحة `/profile`.
  3. في قسم «الجلسات النشطة»، تأكد من ظهور جلستين: واحدة تحمل علامة «الجلسة الحالية» والأخرى جهاز إضافي.
  4. اضغط زر **«إنهاء الجلسات الأخرى»**.
* **النتيجة المتوقعة:**
  - حذف كافة رموز الجلسات الأخرى من جدول `user_sessions`.
  - التوجه إلى المتصفح الثاني والضغط على أي رابط -> يتم طرده فوراً وتحويله لصفحة الدخول.

---

## المجموعة الثانية: الواجهة العامة والتصميم المتجاوب

### `TC-PUB-01`: فحص الصفحة الرئيسية والهوية العربية RTL
* **الصفحة:** `/`
* **الخطوات:**
  1. افتح الرابط الرئيسي `http://localhost:3000`.
  2. افحص اتجاه الصفحة: تأكد من وجود `dir="rtl"` والخط العربي المميز **IBM Plex Sans Arabic**.
  3. تحقق من قسم المؤشرات السريعة (Counters) وتدرج ظهورها مع التمرير (Scroll Animations).
  4. تحقق من أزرار «انضم إلينا»، «تبرع الآن»، و«طلب مساعدة».
* **النتيجة المتوقعة:**
  - مظهر متناسق، ألوان كحلية وزمردية راقية خالية من أي أخطاء كسر في النصوص أو محاذاة الأيقونات.

---

### `TC-PUB-02`: شريط التنقل المتفاعل (Dynamic Auth Header)
* **الصفحة:** `/` وكافة الصفحات
* **الخطوات:**
  1. عندما تكون **زائراً**: تأكد من ظهور زري «تسجيل الدخول» و«إنشاء حساب»، واختفاء عناصر لوحة المشرف.
  2. عند تسجيل الدخول **كمشرف**: يظهر زر لوحة التحكم، اسم «المشرف»، وأيقونة الإشعارات.
  3. عند تسجيل الدخول **كمستفيد**: يظهر تبويب «طلب مساعدة» ويختفي تبويب إضافة تبرع وتعديل المستودع.
  4. تصغير شاشة المتصفح لأبعاد هاتف محمول (Mobile View < 768px):
     - الضغط على زر القائمة المنسدلة (Hamburger Menu).
* **النتيجة المتوقعة:**
  - فتح وإغلاق القائمة الجانبية بسلاسة وإظهار العناصر المسموحة فقط لكل دور.

---

## المجموعة الثالثة: دورة التبرعات العينية

### `TC-DON-01`: تسجيل تبرع عيني جديد من حساب متبرع
* **الصفحة:** `/donations`
* **الدور:** متبرع (`donor`)
* **الخطوات:**
  1. سجل الدخول بحساب المتبرع `tarek.donor@test.com`.
  2. توجه لصفحة التبرعات `/donations`.
  3. اضغط زر **«+ تسجيل تبرع جديد»**.
  4. تفتح نافذة إدخال بيانات التبرع، املأ البيانات التالية:
     * عنوان التبرع: `كراتين مواد غذائية متكاملة`
     * الفئة: اختر `مواد غذائية`
     * الكمية: `50` | وحدة القياس: `كرتونة`
     * الحالة: `جديد بالكامل`
     * المستودع / الفرع: `مستودع الجيزة المركزي`
     * الموقع / المحافظة: `الجيزة`
     * تاريخ انتهاء الصلاحية: اختر تاريخاً بعد 6 أشهر.
     * وصف وملاحظات: `تحتوي كل كرتونة على زيت وسكر وأرز ومكرونة وبقوليات.`
  5. اضغط زر «تأكيد وإرسال التبرع».
* **النتيجة المتوقعة:**
  - ظهور رسالة نجاح وإغلاق النافذة.
  - إدراج التبرع في قائمة تبرعاتي بكود فريد مثل `DON-0001` وبحالة **«قيد المراجعة» (Pending)**.
  - عدم إضافة الأصناف للمستودع المتاح حتى الآن (في انتظار اعتماد الإدارة).

---

### `TC-DON-02`: منع المستفيد من تسجيل تبرعات (Role Guard)
* **الصفحة:** `/donations`
* **الدور:** مستفيد (`beneficiary`)
* **الخطوات:**
  1. سجل الدخول بحساب المستفيد `saber.beneficiary@test.com`.
  2. افتح صفحة التبرعات `/donations`.
  3. حاول الضغط على زر التبرع أو إرسال طلب تبرع.
* **النتيجة المتوقعة:**
  - يتم تعطيل الزر أو إظهار رسالة رفض قاطعة: «لا يمكنك تسجيل تبرع لأن حسابك مسجل كمستفيد، وليس كمتبرع» (الكود `BENEFICIARY_DONATION_FORBIDDEN`).

---

### `TC-DON-03`: مراجعة واعتماد التبرع من قبل الإدارة وإنشاء صنف المستودع تلقائياً
* **الصفحة:** `/admin-requests`
* **الدور:** المشرف العام (`admin`)
* **الخطوات:**
  1. سجل الدخول بحساب المشرف `admin@sanad.com`.
  2. اذهب إلى صفحة طلبات الإدارة `/admin-requests`.
  3. في تبويب **«طلبات التبرعات»**، لاحظ ظهور طلب التبرع `DON-0001` المقدم من طارق منصور.
  4. اضغط زر **«موافقة» (Approve)** الأخضر.
* **النتيجة المتوقعة:**
  - تحول حالة الطلب فوراً إلى **«تمت الموافقة» (Approved)**.
  - ظهور إشعار توست أخضر: «تمت الموافقة على طلب التبرع».
  - **خلف الكواليس المعاملاتية:** يقوم النظام تلقائياً بإنشاء صنف جديد في جدول المخزون `inventory_items` باسم `كراتين مواد غذائية متكاملة` وبكمية متاحة `50 كرتونة` مرتبطة بهذا التبرع!

---

### `TC-DON-04`: رفض تبرع غير مطابق للمواصفات
* **الصفحة:** `/admin-requests`
* **الدور:** المشرف العام (`admin`)
* **الخطوات:**
  1. قدم طلباً تجريبياً لتبرع عيني تالف أو غير صالح.
  2. من لوحة المشرف في `/admin-requests`، اضغط زر **«رفض» (Reject)**.
* **النتيجة المتوقعة:**
  - تحول حالة التبرع إلى «مرفوض».
  - عدم إضافة أي كمية للمستودع.
  - تسجيل حركة الرفض في سجل الرقابة `audit_logs`.

---

### `TC-DON-05`: فلاتر التبرعات وعرض التفاصيل (Details Drawer)
* **الصفحة:** `/donations`
* **الخطوات:**
  1. استخدم حقل البحث بالكلمات المفتاحية (ابحث عن `غذائية`).
  2. فلتر حسب الفئة (اختر `أجهزة` أو `ملابس` ثم `مواد غذائية`).
  3. اضغط على بطاقة أي تبرع لفتح «لوحة التفاصيل الجانبية (Drawer)».
* **النتيجة المتوقعة:**
  - استجابة سريعة وتحديث فوري للبطاقات دون إعادة تحميل الصفحة.
  - عرض كامل بيانات المتبرع، الكمية، حالة التبرع، وتاريخ الوصول في الدرج الجانبي.

---

## المجموعة الرابعة: إدارة المستودع والمخزون

### `TC-INV-01`: استعراض المخزون وتصنيف الحالات
* **الصفحة:** `/inventory`
* **الدور:** المشرف العام أو موظف المستودع
* **الخطوات:**
  1. افتح صفحة المستودع `/inventory`.
  2. تأكد من ظهور الصنف المعتمد في `TC-DON-03` مع كميته المتاحة (50).
  3. تحقق من شارات الحالة الملونة:
     - أخضر: **متاح (Available)**
     - برتقالي: **مخزون منخفض (Low Stock)**
     - أحمر: **نفد المخزون (Out of Stock)**
     - أزرق: **فائض (Surplus)**
* **النتيجة المتوقعة:**
  - عرض دقيق للكميات الإجمالية، المتاحة، والمحجوزة للتوزيع.

---

### `TC-INV-02`: إضافة صنف مخزني يدوياً
* **الصفحة:** `/inventory`
* **الدور:** المشرف العام
* **الخطوات:**
  1. اضغط زر **«+ إضافة صنف جديد للمخزن»**.
  2. أدخل البيانات:
     * اسم الصنف: `بطاطين شتوية صوف`
     * الفئة: `كساء ومفروشات`
     * الكمية الإجمالية: `100` | الوحدة: `بطانية`
     * حد التنبيه بنقص المخزون (Low Stock Threshold): `15`
     * المستودع: `المخزن الرئيسي - القاهرة`
     * الحالة الفنية: `ممتازة`
  3. اضغط «حفظ الصنف في المستودع».
* **النتيجة المتوقعة:**
  - إضافة الصنف بنجاح وإدراجه في جدول المخزون.
  - ظهور الصنف كـ «متاح» لأن الكمية (100) أكبر بكثير من حد التنبيه (15).

---

### `TC-INV-03`: تعديل بيانات الصنف وحدود التنبيه بنقص المخزون
* **الصفحة:** `/inventory`
* **الخطوات:**
  1. اختر الصنف السابق واضغط زر «تعديل».
  2. قم بتعديل الكمية المتاحة لتصبح `10` وحدات فقط (أقل من حد التنبيه 15).
  3. اضغط «تحديث».
* **النتيجة المتوقعة:**
  - تحديث بيانات الصنف فوراً.
  - تحول شارة الصنف تلقائياً إلى اللون البرتقالي **«مخزون منخفض»**.
  - ظهور تنبيه فوري في جرس إشعارات شريط الرأس ينبه مسؤولي النظام بوجود صنف منخفض في المستودع!

---

## المجموعة الخامسة: سجل المستفيدين والذكاء الاصطناعي

### `TC-BEN-01`: استعراض جدول المستفيدين وحجب البيانات الحساسة (PII Masking)
* **الصفحة:** `/beneficiaries`
* **الدور:** المشرف العام
* **الخطوات:**
  1. افتح صفحة سجل المستفيدين `/beneficiaries`.
  2. افحص أعمدة الجدول الخاصة بـ «الرقم القومي» و«رقم الهاتف».
* **النتيجة المتوقعة:**
  - تطبيق سياسة الحجب الآمن للمعلومات الشخصية المصرية:
    - الرقم القومي يظهر محجوباً بالصيغة: `295*********14` (إخفاء الأرقام الوسطى وإظهار البداية والنهاية فقط).
    - رقم الهاتف يظهر محجوباً: `010*****678`.
  - عدم تسريب الأرقام القومية كاملة في العرض العام التزاماً بمعايير الخصوصية.

---

### `TC-BEN-02`: استخراج بيانات الحالة بالذكاء الاصطناعي (AI Case Notes Extraction)
* **الصفحة:** `/beneficiaries`
* **الدور:** المشرف العام
* **الخطوات:**
  1. اضغط زر **«+ تسجيل مستفيد جديد»**.
  2. في نافذة التسجيل، اضغط زر **«المساعد الذكي لاستخراج البيانات» (AI Assistant)**.
  3. يفتح قسم المساعد الذكي؛ اضغط زر **«استخدام نص تجريبي»** ليتم وضع تقرير زيارة ميدانية اجتماعي عربي غير مهيكل:
     > *"زار الباحث أسرة المواطن محمود أحمد الشريف (هاتف: 01012345678، رقم قومي: 28904011234567) بمركز أوسيم بمحافظة الجيزة. الأسرة مكونة من 6 أفراد بينهم 4 أطفال، منهم 3 في سن المدرسة. تسكن الأسرة في شقة إيجار جديد. يعمل رب الأسرة باليومية في البناء ويعاني من دخل متقطع لا يتجاوز 2500 جنيه شهرياً. الزوجة مريضة سكر وتحتاج علاج شهري مستمر. تحتاج الأسرة بشكل عاجل إلى طرد مواد غذائية، وكسوة مدرسية للأطفال، ودعم علاجي للزوجة."*
  4. اضغط زر **«تحليل واستخراج البيانات بالذكاء الاصطناعي»**.
* **النتيجة المتوقعة:**
  - يتم إرسال النص إلى واجهة الذكاء الاصطناعي (مع وجود بديل لغوي محلي فوري إن كان الاتصال الخارجي غير متوفر).
  - استخراج البيانات المنظمة وعرضها في بطاقة مراجعة:
    * الاسم: محمود أحمد الشريف
    * الرقم القومي: 28904011234567
    * الهاتف: 01012345678
    * عدد أفراد الأسرة: 6 | عدد الأطفال: 4
    * الدخل الشهري: 2500
    * الاحتياجات المستخرجة: كرتونة مواد غذائية، كسوة مدرسية، علاج سكر.
  - **ظهور التنبيه الاسترشادي الإلزامي:**
    `«تنبيه استرشادي: تم استخراج هذه البيانات بالذكاء الاصطناعي لمساعدة موظف الحالة، وتبقى المراجعة البشرية والقرار النهائي مسؤولية الأخصائي المختص»`.
  5. اضغط زر **«تطبيق ونقل البيانات للنموذج»**.
* **النتيجة:** تعبئة كافة حقول النموذج بالبيانات المستخرجة بنقرة زر واحدة!

---

### `TC-BEN-03`: حفظ ملف المستفيد واختبار التحقق من صحة الرقم القومي المصري
* **الصفحة:** `/beneficiaries`
* **الخطوات:**
  1. في نموذج المستفيد، جرب وضع رقم قومي خاطئ (10 أرقام فقط أو يحتوي على حروف).
  2. اضغط حفظ -> ظهور خطأ يوضح وجوب إدخال 14 رقماً قومياً مصرياً صالحاً.
  3. ضع الرقم القومي الصحيح المكون من 14 رقماً: `28904011234567`.
  4. اضغط «حفظ ملف المستفيد».
* **النتيجة المتوقعة:**
  - إنشاء ملف المستفيد بنجاح، وتوليد كود مرجعي فريد تلقائياً مثل `BEN-0001`.
  - إدراج المستفيد في جدول الحالات بحالة **«قيد المراجعة» (Pending)**.

---

### `TC-BEN-04`: إدارة الاحتياجات المنظمة للحالة (Structured Needs)
* **الصفحة:** `/beneficiaries`
* **الخطوات:**
  1. اضغط على اسم المستفيد `محمود أحمد الشريف` لفتح ملف الحالة الشامل (Drawer).
  2. انتقل لتبويب **«الاحتياجات» (Needs)**.
  3. اضغط زر **«+ إضافة احتياج جديد»**.
  4. أدخل البيانات:
     * عنوان الاحتياج: `كرتونة مواد غذائية شهرية`
     * الفئة: `مواد غذائية`
     * الكمية المطلوبة: `2` | الوحدة: `كرتونة`
     * الأولوية: `عاجلة (Urgent)`
  5. اضغط حفظ.
* **النتيجة المتوقعة:**
  - تسجيل الاحتياج في جدول `beneficiary_needs` بحالة **«مفتوح» (Open)** وشارة أولوية حمراء (عاجلة).
  - إمكانية ربط هذا الاحتياج لاحقاً بعملية التوزيع ليتم إغلاقه تلقائياً عند التسليم!

---

## المجموعة السادسة: محرك القواعد ومكتب التحقق

### `TC-RULE-01`: فحص التوصية الذكية للمستفيد ومطابقة المخزون
* **الصفحة:** `/beneficiaries`
* **الخطوات:**
  1. في ملف المستفيد (Drawer)، افتح تبويب **«التوصية الاسترشادية» (Recommendation)**.
  2. اضغط زر «تحديث التوصية».
* **النتيجة المتوقعة:**
  - تشغيل محرك القواعد وحساب درجة الأولوية بناءً على:
    * حجم الأسرة (6 أفراد) -> رفع الأولوية.
    * الدخل المنخفض (2500 جنيه) -> رفع الأولوية.
    * الحالة المرضية للزوجة ووجود أطفال في سن الدراسة -> رفع الأولوية.
  - حساب مستوى الأولوية العام: **«عالية» (High) أو «عاجلة» (Urgent)**.
  - كتابة مبررات القرار بوضوح للموظف: («دخل الأسرة أقل من خط الفقر»، «عدد الأطفال يتجاوز 3»، «وجود حالة مرضية»).
  - استعراض الأصناف المتاحة بالمستودع والتي تطابق احتياجات الحالة (مثل كراتين المواد الغذائية).

---

### `TC-VERIF-01`: كشف المساعدات السابقة خلال 30 يوماً (Anti-Double-Dipping Warning)
* **الصفحة:** `/verification`
* **الدور:** باحث اجتماعي / مشرف التحقق
* **الخطوات:**
  1. افتح صفحة مكتب التحقق `/verification`.
  2. في شريط البحث السريع، اكتب اسم المستفيد أو رقمه القومي.
  3. اختر ملف الحالة واضغط عرض الملف الكامل.
  4. افحص بطاقة **«فحص المساعدات السابقة (خلال 30 يوماً)»**:
     - إذا كانت الحالة جديدة: تظهر علامة خضراء: «لم تتلق الأسرة أي مساعدات خلال الـ 30 يوماً الماضية».
     - إذا تم تنفيذ توزيع للحالة قبل أسبوعين مثلاً: يظهر شريط تحذيري كهرماني: `«تنبيه: تلقت هذه الأسرة طرد مساعدات بتاريخ ... (خلال أقل من 30 يوماً)»`.
* **النتيجة المتوقعة:**
  - تقديم معلومة واضحة وكاشفة للأخصائي الاجتماعي لاتخاذ القرار المناسب دون حظر النظام التلقائي للمساعدة إذا كانت الحالة تستدعي تدخلاً طارئاً.

---

### `TC-VERIF-02`: اعتماد ملف المستفيد (Verification Approval)
* **الصفحة:** `/verification`
* **الخطوات:**
  1. في صفحة فحص الحالة `/verification`، بعد مراجعة تقرير الدخل والبحث الميداني.
  2. اضغط زر **«اعتماد المستفيد» (Approve)**.
* **النتيجة المتوقعة:**
  - تحول حالة المستفيد فوراً إلى **«معتمد» (Verified)**.
  - تسجيل معرّف الموظف ووقت التحقق في قاعدة البيانات.
  - إمكانية جدولة وتوجيه أوامر التوزيع الميداني له رسمياً.

---

## المجموعة السابعة: عمليات التوزيع الميداني والخصم الذري

### `TC-DIST-01`: إنشاء وتأكيد أمر توزيع جديد وحسم المخزون الذري
* **الصفحة:** `/distributions`
* **الدور:** المشرف العام / منسق التوزيع
* **الخطوات:**
  1. توجه لصفحة التوزيعات الميدانية `/distributions`.
  2. اضغط زر **«+ إنشاء عملية توزيع جديدة»**.
  3. تفتح نافذة معالج التوزيع:
     * اختر المستفيد المعتمد: `محمود أحمد الشريف`.
     * لاحظ ظهور ملخص فوري لبياناته واحتياجاته المفتوحة وتاريخ الدعم السابق في المعالج.
  4. في قسم الأصناف المطلوب تسليمها، اضغط «+ إضافة صنف»:
     * اختر صنف المخزن: `كراتين مواد غذائية متكاملة` (المتاح: 50).
     * أدخل الكمية: `2`.
     * في خانة «ربط باحتياج المستفيد»: اختر احتياجه المسجل `كرتونة مواد غذائية شهرية`.
  5. اختر حالة التوزيع: **«تم التسليم فورياً» (Completed)**.
  6. اكتب موقع التسليم: `مركز توزيع الجيزة`.
  7. اضغط زر **«تأكيد وحسم المخزون فورياً»**.
* **النتيجة المتوقعة:**
  - تنفيذ العملية كمعاملة ذرية آمنة (Atomic Transaction):
    1. خصم الكمية (2) فوراً من المستودع لتصبح الكمية المتاحة (48).
    2. تحديث احتياج المستفيد لتصبح الكمية المكتملة (2 من 2) وإغلاق الاحتياج كـ **«مكتمل» (Fulfilled)**.
    3. إنشاء أمر التوزيع برقم مرجعي مميز مثل `DIST-0001`.
  - ظهور نافذة **«سند استلام التوزيع الرسمي» (Voucher Modal)** تلقائياً محتوياً على كامل بيانات التسليم وجاهزاً للطباعة والتوقيع.

---

### `TC-DIST-02`: فحص منع التوزيع الزائد (Insufficient Stock Prevention)
* **الصفحة:** `/distributions`
* **الخطوات:**
  1. افتح نافذة إنشاء توزيع جديد مرة أخرى.
  2. اختر أي مستفيد.
  3. اختر صنفاً مخزنياً متاح منه 10 قطع فقط.
  4. في خانة الكمية، اكتب `25` قطعة.
  5. حاول الضغط على «تأكيد وحسم المخزون».
* **النتيجة المتوقعة:**
  - ظهور تنبيه أحمر فوري في النموذج: «تحذير: الكمية المطلوبة تتجاوز المتاح في المخزن (10)!».
  - رفض الخادم للطلب برمز خطأ `409 Conflict` وكود `INSUFFICIENT_STOCK`.
  - منع حدوث أي كميات سالبة في قاعدة البيانات نهائياً.

---

### `TC-DIST-03`: إلغاء أمر توزيع مع الاسترجاع التلقائي للأصناف إلى المخزن
* **الصفحة:** `/distributions`
* **الخطوات:**
  1. في جدول التوزيعات، ابحث عن أمر التوزيع المنفذ `DIST-0001` (الذي تم خصم 2 كرتونة فيه).
  2. اضغط زر «خيارات» ثم اختر **«إلغاء التوزيع» (Cancel)**.
  3. أكد رسالة التأكيد.
* **النتيجة المتوقعة:**
  - تحول حالة التوزيع إلى **«ملغى» (Cancelled)**.
  - **استرداد المخزون تلقائياً:** إعادة إضافة الكمية (2) إلى المخزون المتاح في جدول `inventory_items` ليعود الرصيد إلى (50 كرتونة)!
  - إعادة فتح احتياج المستفيد المرتبط.
  - إظهار رسالة توست: «تم إلغاء عملية التوزيع واسترجاع الأصناف للمستودع بنجاح».

---

### `TC-DIST-04`: استعراض وطباعة سند التسليم (Printable Voucher)
* **الصفحة:** `/distributions`
* **الخطوات:**
  1. اضغط على رقم أي توزيع مكتمل في الجدول (أو أيقونة السند).
  2. تفتح نافذة سند التوزيع المصمم للميدان.
  3. اضغط زر **«طباعة السند» (Print Voucher)**.
* **النتيجة المتوقعة:**
  - فتح نافذة طباعة المتصفح مع تنسيق خاص بالطباعة (Print Stylesheet) يخفي عناصر التحكم ويظهر السند الرسمي بختم المنصة وتوقيعات الاستلام بشكل احترافي.

---

## المجموعة الثامنة: طلبات المساعدة الذاتية للمستفيدين

### `TC-REQ-01`: تقديم طلب مساعدة ذاتية من قبل المستفيد
* **الصفحة:** `/request-assistance`
* **الدور:** مستفيد (`beneficiary`)
* **الخطوات:**
  1. سجل الدخول بحساب المستفيد `saber.beneficiary@test.com`.
  2. توجه إلى صفحة «طلب مساعدة» `/request-assistance`.
  3. لاحظ تعبئة اسم المستفيد تلقائياً.
  4. املأ بيانات الطلب:
     * الصنف أو المساعدة المطلوبة: `أدوات مدرسية وحقائب`
     * الفئة: `أدوات مدرسية`
     * الكمية المطلوبة: `3` | الوحدة: `حقيبة`
     * وصف الاحتياج ومبرراته: `لبدء العام الدراسي لأطفالي الثلاثة في المرحلة الابتدائية.`
  5. اضغط زر «إرسال طلب المساعدة».
* **النتيجة المتوقعة:**
  - حفظ الطلب برقم مرجعي مثل `REQ-0001` وبحالة **«قيد المراجعة» (Pending)**.
  - ظهوره الفوري في أسفل الصفحة في قائمة «طلباتي السابقة» مع شارة صفراء توضح أنه قيد الانتظار.

---

### `TC-REQ-02`: منع تكرار نفس الطلب المعلق (Duplicate Request Guard)
* **الصفحة:** `/request-assistance`
* **الدور:** مستفيد
* **الخطوات:**
  1. بنفس حساب المستفيد، أعد ملء النموذج لطلب نفس الصنف `أدوات مدرسية وحقائب` بنفس الفئة بينما الطلب الأول لا يزال «قيد المراجعة».
  2. اضغط «إرسال طلب المساعدة».
* **النتيجة المتوقعة:**
  - رفض الطلب فوراً برسالة خطأ واضحة: `«يوجد لديك طلب مساعدة معلق بالفعل لنفس هذا الصنف»` (الكود `DUPLICATE_ASSISTANCE_REQUEST`).
  - حماية النظام وقاعدة البيانات من إغراق الطلبات المتكررة.

---

### `TC-REQ-03`: مراجعة واعتماد طلب المساعدة من قبل الإدارة وتحويله لاحتياج رسمي
* **الصفحة:** `/admin-requests`
* **الدور:** المشرف العام
* **الخطوات:**
  1. سجل الدخول بحساب المشرف `admin@sanad.com`.
  2. اذهب إلى `/admin-requests` وانتقل إلى التبويب الثالث: **«طلبات المساعدة»**.
  3. يظهر طلب صابر عبد الله `REQ-0001` الخاص بـ `3 حقائب مدرسية`.
  4. اضغط زر **«موافقة» (Approve)**.
* **النتيجة المتوقعة:**
  - تحديث حالة الطلب إلى **«تمت الموافقة» (Approved)**.
  - **الربط الذكي:** يقوم الخادم تلقائياً بإنشاء احتياج جديد في جدول احتياجات المستفيد `beneficiary_needs` ليظهر فوراً في ملف الحالة وجدول التوزيعات الميدانية!

---

## المجموعة التاسعة: إدارة المستخدمين والصلاحيات

### `TC-ROLE-01`: استعراض وتبديل أدوار المستخدمين (User Role Switcher)
* **الصفحة:** `/admin-requests`
* **الدور:** المشرف العام
* **الخطوات:**
  1. في صفحة `/admin-requests`، انتقل إلى التبويب الثاني: **«إدارة المستخدمين والأدوار»**.
  2. استعرض قائمة المستخدمين المسجلين في النظام.
  3. ابحث عن أي مستخدم مسجل كمتبرع، وفي عمود الدور قم بتغيير القائمة المنسدلة من `متبرع` إلى `مستفيد` أو العكس.
* **النتيجة المتوقعة:**
  - إرسال طلب `PATCH /api/admin/users/:id/role` مصحوباً برمز الحماية CSRF.
  - تحديث الدور بنجاح وظهور إشعار تأكيد.
  - تسجيل حركة `user_role_changed` في سجل الرقابة.

---

### `TC-ROLE-02`: حماية حساب المشرف من تخفيض الصلاحية لنفسه (Self-Demotion Guard)
* **الصفحة:** `/admin-requests` (أو عبر الواجهة البرمجية)
* **الخطوات:**
  1. حاول من حساب المشرف تغيير رتبة حسابك أنت (`admin@sanad.com`) إلى متبرع أو مستفيد.
* **النتيجة المتوقعة:**
  - رفض العملية فوراً برسالة حماية أمنية: «لا يمكنك تخفيض أو تغيير صلاحية حساب المشرف الخاص بك منعاً لفقدان الوصول للنظام» (الكود `CANNOT_DEMOTE_SELF`).

---

## المجموعة العاشرة: لوحة المؤشرات، التقارير وسجل الرقابة

### `TC-DASH-01`: فحص العدادات التشغيلية الحية (Live KPI Counters)
* **الصفحة:** `/dashboard`
* **الدور:** المشرف العام
* **الخطوات:**
  1. افتح لوحة التحكم الرئيسية `/dashboard`.
  2. قارن أرقام البطاقات الأربع العليا:
     * إجمالي التبرعات المستلمة.
     * الأصناف المتاحة في المخزون.
     * عدد المستفيدين المسجلين.
     * عمليات التوزيع المنفذة.
* **النتيجة المتوقعة:**
  - مطابقة الأرقام بنسبة 100% مع العمليات الفعلية التي قمت بها في الاختبارات السابقة.

---

### `TC-DASH-02`: فحص جداول التنبيهات الذكية والأولويات
* **الصفحة:** `/dashboard`
* **الخطوات:**
  1. انزل إلى قسم جداول المراقبة في اللوحة:
     - جدول **«تنبيهات انخفاض المخزون»**: تأكد من ظهور الأصناف التي انخفضت كميتها عن الحد الأدنى.
     - جدول **«الحالات ذات الأولوية العاجلة»**: تأكد من ظهور المستفيدين الحاصلين على تقييم `Urgent` أو `High`.
     - جدول **«أحدث عمليات التوزيع»**: تأكد من ظهور أوامر التسليم الأخيرة.
* **النتيجة المتوقعة:**
  - ترتيب الجداول وعرض البيانات المحدثة بدقة مع روابط مباشرة للانتقال لملف الحالة أو الصنف.

---

### `TC-DASH-03`: استخراج التقارير والتحليلات الجغرافية الخالية من البيانات الحساسة
* **الصفحة:** `/dashboard` (قسم التقارير)
* **الخطوات:**
  1. في لوحة التحكم، اذهب لقسم التحليلات والتقارير.
  2. حدد نطاقاً زمنياً (من تاريخ - إلى تاريخ) أو فئة معينة.
  3. اضغط «تحديث التقرير».
* **النتيجة المتوقعة:**
  - استخراج رسوم بيانية وجداول مجمعة تبين:
    - نسب التوزيع حسب المحافظات.
    - أكثر الاحتياجات طلباً.
    - معدل دوران المخزون.
  - خلو التقارير والواجهات التحليلية تماماً من الأسماء أو الأرقام القومية حفاظاً على سرية بيانات الأسر.

---

### `TC-DASH-04`: مراجعة سجل الرقابة والتدقيق الأمني (Security Audit Log)
* **الصفحة:** `/dashboard` أو `GET /api/admin/audit-logs`
* **الدور:** المشرف العام
* **الخطوات:**
  1. في أسفل لوحة التحكم، افحص نافذة **«سجل النشاط والرقابة الأمني» (Audit Trail)**.
  2. راجع السجلات المرصودة حديثاً.
* **النتيجة المتوقعة:**
  - ظهور سجلات موثقة لكل العمليات التي قمت بتنفيذها مع:
    - اسم المستخدم وهوية الحساب.
    - نوع العملية (مثل: `login_success`, `donation_submitted`, `beneficiary_created`, `distribution_created`, `mfa_enabled`).
    - عنوان الـ IP ووقت العملية بدقة.
    - نتيجة العملية (ناجحة / فاشلة).
    - تفاصيل البيانات الوصفية (Metadata).

---

## 🎯 ملخص رحلة الاختبار المتكاملة السريعة (End-to-End Primary Journey)

إذا كنت ترغب في تجربة «دورة العمل الكاملة» في 5 دقائق متواصلة، اتبع هذا السيناريو:

```text
1. الدخول بحساب المشرف: admin@sanad.com / Sanad@2026
2. تسجيل الدخول والاطلاع على لوحة المؤشرات (/dashboard)
3. الذهاب للتبرعات (/donations) وتسجيل تبرع جديد (50 كرتونة تموينية)
4. الذهاب لطلبات الإدارة (/admin-requests) والموافقة على التبرع ليتحول لمخزون
5. الذهاب لسجل المستفيدين (/beneficiaries) وتجربة زر الذكاء الاصطناعي لاستخراج بيانات حالة وحفظها
6. مراجعة التوصية الاسترشادية للحالة وإضافة احتياج لكرتونة تموينية
7. التوجه لمكتب التحقق (/verification) واعتماد الحالة وفحص مؤشر الـ 30 يوماً
8. التوجه للتوزيعات (/distributions) وإنشاء أمر توزيع وحسم 2 كرتونة فورياً
9. طباعة واستعراض سند التوزيع الرسمي (Voucher)
10. العودة للوحة المؤشرات (/dashboard) وملاحظة تحديث العدادات وسجل الرقابة تلقائياً!
```
